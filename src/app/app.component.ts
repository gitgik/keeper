import { Component } from '@angular/core';

@Component({
  // Attach the component to the DOM using this selector
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css']
})
export class AppComponent {
  title = 'app works!';
}
